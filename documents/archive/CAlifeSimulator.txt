0x1024A86E -> 0x1038602A -> 0x10384540 -> CAlifeSimulator::CAlifeSimulator

0x103844F0 (game_sv_mp::create_unknown -> create game & create alife)
в дедике при нормальном запуске мп данная функция вообще не вызывается...и правда нахуя вызывать всякую важную бля поебень =/

0x10385D20 ; int __stdcall xrServer__Connect(int, char *Dst)
данная функция в дедике вызывается...
classid of SV_DM:
53565F44
4D202020

но для дедика вызывается 0x10383C70 являющийся game_sv_Deathmatch::Create

следовательно, разделение сингла с алафом и мп без алайфа идёт как раз в xrServer::Connect, после получения класса сервера (SV_DM вместо SV_SINGLE)

так вот...xrServer::Connect получает инфу о классе с идом SV_SINGL или SV_DM и создаёт через xrFactory_Create класс по инфе...далее вызывается функция этого класса:
	SV_SINGL:
0x103844F0 game_sv_GameState::PreCreate_SINGLE
она создаёт GameState и алайф и всё крута...главное ниже
	SV_DM:
0x10383C70 game_sv_Deathmatch::Create
она создаёт всякий бред для мп дм режима и потом вызывает 
0x10389360 game_sv_GameState::PreCreate_MP которая тоже создаёт GameState но без алайфа и с поправками для мп

ТАК ВОТ...реализация алайфа в мп...оооочень надеюсь что этот вариант проканает....ибо бля я ему голову снесу если не проканает....всё разнесу нахуй...ну да ладно...и так реализация алайфа в мп:
берем game_sv_GameState::PreCreate_MP и после создания, в конце перед завершением функции вызываем нашу функцию (т.е. ставим call хук на нашу функцию) как параметры толкнём класс game_sv_GameState...далее в нашей функции выполняем подключение алайфа из game_sv_GameState::PreCreate_SINGLE т.е. выделение памяти, создание класса на выделенной памяти, запись указателя на класс алайфа в класс game_sv_GameState...и конец попробуем как в сингл функции - вызов функции класса game_sv_GameState по смещению + 8 с параметром 1 потом возвращаемся в game_sv_GameState::PreCreate_MP и там не учитывая что вернула наша функция возвращаем 0...в итоге мы должны получить запуск мп режимов с алайфом...пробуем (16.05.2011 - 17:23)

10096290